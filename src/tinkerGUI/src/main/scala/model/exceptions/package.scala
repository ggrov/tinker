package tinkerGUI.model

/** ==TinkerGUI model exceptions package==
  * Provides custom exceptions for the psgraph model of Tinker's interface.
	*
	* {{{
	* 	...
	*   try {
	*   	// some actions on the psgraph model that may fail
	*   } catch {
	*   	case e:PSGraphModelException => println(e.msg)
	*   }
	*   ...
	* }}}
	*
  */
package object exceptions {

}
